#!/usr/bin/python3

"""Module for matrix devided function"""

def matrix_divided(matrix, div):
    """this is a function for deviding all
    elements of a matrix"""

    try:
        x = a + 10
        y = b + 10
    except TypeError:
        if not y:
            return "b must be an integer"
        if not x:
            return "a must be an integer"
    return int(a) + int(b)

if __name__ == "__main__":
    import doctest
    doctest.testmod()
